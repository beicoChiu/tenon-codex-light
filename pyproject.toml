[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "tenon-signal-light"
version = "0.1.0"
description = "Local-first ambient workspace signal layer for Beflo Tenon and AI-assisted workflows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["bleak>=0.22"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
tenon-hook-state = "tenon_signal_light.hook_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
